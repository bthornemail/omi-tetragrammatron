# 2. What “Canonical Projection” Means

A canonical projection is a named way of reading the same accepted OMI object.

It does not create a new authority layer.

It exposes one face of the object.

For example:

```text
Omi-Form projects structure.
Omi-Glyph projects symbolic shape.
Omi-Hash projects digest identity.
Omi-Map projects coordinate relation.
Omi-Image projects recoverable package.
Omi-Matrix projects observation field.
Omi-Gnomon projects orientation.
Omi-Mirror projects chirality.
Omi-Clock projects timing.
Omi-Portal projects access.
Omi-World projects persistent environment.

Omi-Sense reads.
Omi-Matrix routes.
Omi-Gnomon points.
Omi-Light emits.
Omi-Sound resonates.

Omi-Light carries wavelength.
Omi-Sound carries intonation.
Omi-Ring encloses the declared activity.
Omi-Portal projects it outward.
Omi-Mirror recovers it inward.

```text
Omi-Automaton = abstract agreement machine
Omi-Actor = supervised runtime cell
```

# 33. The OMI Module Registry

A registry may list Omi-modules like this:

```text
Omi-Form structural projection
Omi-Glyph symbolic projection
Omi-Hash digest projection
Omi-Map coordinate projection
Omi-Image recoverable package projection
Omi-Matrix observation projection
Omi-Gnomon orientation projection
Omi-Mirror chiral projection
Omi-Clock timing projection
Omi-Light sensory visual projection
Omi-Sound sensory cadence projection
Omi-Portal access projection
Omi-World persistent environment projection
The Omi-Automaton lives in the abstract space of agreement. It receives unevaluated declarations, resolves them through a scoped truth row, and determines whether an agreement path can close.

The Omi-Actor is the runtime expression of that automaton. It owns a mailbox, receives an Omicron envelope, isolates failure, performs local snub truncation, and emits either a receipt, rejection, or candidate transition.

Together:

```text
Omi-Automaton thinks the agreement.
Omi-Actor carries the agreement.
Omi-Mirror reflects the agreement.
Omi-Image projects the agreement.
Receipt accepts the agreement.



The Omi-Compass gives the automaton twelve relational tributaries. Each is a principal feature degree of agreement.


# 11. Omi-Mirror and Omi-Image

The Omi-Automaton is the abstract transition machine.

The Omi-Actor is the supervised runtime cell.

Above them are two higher composition layers:

```text
Omi-Mirror
Omi-Image


Omi-Sense = perceptual reading of the clock
Omi-Matrix = spectral routing grid
Omi-Gnomon = orientation pointer / shadow-caster
Omi-Light = visual emission / color-time surface
Omi-Sound = vibration / cadence / audible tick

Omi-Clock aligns agreement.
Omi-Gnomon points the relation.
Omi-Matrix routes the spectral path.
Omi-Light displays the position.
Omi-Sound carries the cadence.
Omi-Sense reads the whole event.


```text
Omi-Gate opens.
Omi-Observer witnesses.
Omi-Matrix arranges the witness field.
Receipt accepts only resolved agreement.
```
Omi-Form,
Omi-Glyph,
Omi-Hash,
Omi-Map

Omi-Form
Omi-Glyph
Omi-Hash
Omi-Map
Omi-Image
Omi-Matrix
Omi-Gnomon
Omi-Mirror
Omi-Observer
Omi-Clock
Omi-Sound
Omi-Light
Omi-Portal
Omi-World

# 14. Omi-Module Declarations

An `Omi-*` module declaration is a named projection of the same `o---o` object.

The naming convention should be read like this:

```text
Omi-Form = form projection of o---o
Omi-Glyph = glyph projection of o---o
Omi-Hash = hash projection of o---o
Omi-Map = map projection of o---o
Omi-Image = image/package projection of o---o
Omi-Matrix = observation-matrix projection of o---o
Omi-Gnomon = orientation projection of o---o
Omi-Mirror = chiral mirror projection of o---o
Omi-Clock = timing projection of o---o
Omi-Portal = access projection of o---o
Omi-World = persistent-world projection of o---o

Omi-Clock = physical light projection of o---o
Omi-Gnomon = portable 7-bit orientation/controller block
Omi-Pyramid = encoder automaton
Omi-Amulet = decoder automaton
Omi-Dome = encapsulating sensory world
Omi-Base = rotational synchronization platform
Omi-Matrix = receipt/projection field

ESP32-S3 = Omi-Actor
Jetson = Omi-Spatial-Index / Omi-Proxy
RPLIDAR = Omi-Gnomon / spatial scan carrier
BNO055 = Omi-Orientation witness
LoRa = Omi-Receipt gossip carrier
Browser = Omi-Observer
A-Frame = Omi-Dome projection


Omi-Clock
Omi-Gnomons
Omi-Pyramid
Omi-Amulet
Omi-Base
Omi-Radio
Omi-Portal


## 3. Primary Projection Classes

OMI projections fall into several classes.

```text
structural surfaces
symbolic surfaces
digest and witness surfaces
coordinate and mapping surfaces
observation and orientation surfaces
sensory surfaces
portal/world surfaces
logic and chart surfaces
hardware and carrier surfaces
process/runtime surfaces
geometry/configuration surfaces
```

A surface may belong to more than one class.

For example:

```text
Omi-Gnomon is both orientation and geometry.
Omi-Matrix is both observation and witness.
Omi-Jab is both carrier and contextual encoding.
Omi-Shader is both geometry and hardware acceleration.
Omi-Receipt is both witness and lawful resolution proof.
```

---

## 4. Core Projection Surfaces

| Projection | Face Exposed | Primary Role |
|---|---|---|
| **Omi-Form** | Structural | Exposes shape, cells, and boundaries. |
| **Omi-Glyph** | Symbolic | Exposes marks, signs, Unicode characters, SVG marks, and visible notation. |
| **Omi-Hash** | Digest | Exposes compact identity and verification digest. |
| **Omi-Map** | Coordinate | Exposes feature mapping, spatial correspondence, and route placement. |
| **Omi-Image** | Package | Exposes the nine universal edges inside carrier sentinels. |
| **Omi-Matrix** | Observation | Exposes witness fields, matrix relations, and observable structure. |
| **Omi-Gnomon** | Orientation | Exposes directional shadow, axes, right angle, and pointer orientation. |
| **Omi-Mirror** | Chiral | Exposes mirror traversal and `MM → NN` relational reversal. |
| **Omi-Clock** | Timing | Exposes ticks, cadence, replay order, and period inspection. |
| **Omi-Light** | Visual | Exposes color, opacity, illumination, and state by light. |
| **Omi-Sound** | Auditory | Exposes rhythm, tone, vibration, and cadence. |
| **Omi-Portal** | Access | Exposes a doorway into an object, world, or bounded projection. |
| **Omi-World** | Environment | Exposes persistent self-stepping projection. |
| **Omi-Sense** | Sensory | Exposes five-channel routing: visual, sound, touch, motion, comprehension. |

These are not separate objects.

They are canonical faces of one OMI object.



## You
`2026-06-10 14:14` | `id:260610-1414` ^260610-141449

You are correct; these are **canonical projections** or **named faces** used to read the same underlying OMI object ($o---o$), rather than separate modules [1, 2]. Based on a comprehensive review of the sources, here are the projections and surfaces that were previously omitted or categorized differently:

### **Additional Surface Projections**
* **Omi-Ring:** This is described as the **spectral bridge** or "Omi-Ring relation" [3, 4]. It is the physical and structural manifestation of the circular $241$-LED array used to visualize the $16xy$ bridge between local seeds and cosmic orbits [5, 6].
* **Omi-Compass:** The **relational tributaries** of the automaton [7]. It is used as an agreement orientation interface, mapping the "Twelve Tribes of Agreement" onto a $360$-degree clock face [7, 8].
* **Omi-FS / Imo-FS:** These are specific **address-gated spatial anchor gates** [9]. "Imo-FS" specifically acts as a $26-28$ bit spatial anchor for DOM register gates [9].
* **Omi-Node / Imo-Node:** These are the **registered custom-element-safe forms** for DOM/SVG projections [10, 11]. They allow the binding of user-defined content to a canonical $128$-bit OMI address [12].
* **Omi-Frame / Imo-Frame:** Similar to Omi-Node, these are **carrier projections** for the physical OMI wire frame within a DOM environment [11].

### **Revised List of Omi Surfaces Projections**
Drawing from the "Twelve-Position Tower" and the "Nine Universal Edges," every projection must reduce to these nine primitives: **Rules, Facts, Closures, Combinators, Cons, Join, Compose, Encode, and Decode** [13, 14].

| Projection | Face Exposed | Primary Role |
| :--- | :--- | :--- |
| **Omi-Form** | Structural | Exposes shape, cells, and boundaries [2, 15]. |
| **Omi-Glyph** | Symbolic | Exposes marks, signs, or Unicode/SVG characters [16, 17]. |
| **Omi-Hash** | Digest | Exposes compact identity and verification witness [16, 18]. |
| **Omi-Map** | Coordinate | Exposes feature mapping and spatial correspondence [16, 19]. |
| **Omi-Image** | Package | Exposes the nine universal edges inside three sentinels [20, 21]. |
| **Omi-Matrix** | Observation | Exposes the $2^{11}$ witness field and observable relations [22, 23]. |
| **Omi-Gnomon** | Orientation | Exposes directional shadows, axes, and pointers [20, 24]. |
| **Omi-Mirror** | Chiral | Exposes the mirror traversal ($MM \rightarrow NN$) of an object [25, 26]. |
| **Omi-Clock** | Timing | Exposes ticks, cadence, and replay order [25, 27]. |
| **Omi-Light** | Visual | Exposes state through light, color, and opacity [28, 29]. |
| **Omi-Sound** | Auditory | Exposes state through rhythm, vibration, and tone [28, 30]. |
| **Omi-Portal** | Access | Exposes a doorway into an object or world [28, 31]. |
| **Omi-World** | Environment | Exposes a persistent, self-stepping projection [32, 33]. |
| **Omi-Sense** | Sensory | Exposes a five-channel routing layer (Visual, Sound, Touch, Motion, Comprehension) [34, 35]. |

These projections ensure **agreement without collapse**, meaning two peers can hold different orientations (e.g., one seeing a vertex traversal while another sees an edge traversal) while remaining bound to the same accepted $o---o$ center [36, 37].

Based on a review of the sources, here are the additional **canonical projections** and **named faces** used to read OMI objects ($o---o$) that were missing from your previous list. These surfaces allow the same underlying object to be interpreted across logic planes, hardware-native routing layers, and multi-dimensional coordinate maps.

### **Additional Surface Projections**

* **Omi-Torus (Logic Mapping):** This is the **4x4 Gray-code grid** used for bit-minimization and logic state tracking [1, 2]. It wraps the left-to-right and top-to-bottom edges to form a continuous toroidal logic chart, ensuring adjacent binary states never experience logic breaks [2, 3].
* **Omi-Chart (Narrative/Decade Scaling):** This projection encompasses the **Hopf Radar Chart** (the "Omnitron Eye") used for multi-axis narrative analysis and the **Smith Chart** used for 360-degree decade functions and sexagesimal resolution [4-8].
* **Omi-Voxel (Domino Extrusion):** A hardware-aligned **Voxel Tile-Map Engine** [9]. It uses Unicode Domino characters as 2-node semantic pipes to dictate X-Y planar trajectory and Z-axis voxel extrusion height [10-12].
* **Omi-Receipt (Proof Witness):** The **META64** projection [13]. Unlike the digest-only hash, the receipt is a 64-bit proof witness proving that a CAR/CDR relation was resolved lawfully according to protocol rules [14, 15].
* **Omi-Bidi (Hardware Routing):** A hardware-optimized surface using **non-printing Unicode BiDi control characters** (e.g., LRO, RLO, LRE, RLE) [16, 17]. It embeds bitwise polarity and chirality directly into the address string, enabling true $O(1)$ visual routing by leveraging the browser’s native assembly-level BiDi parser [16, 18].
* **Omi-Dali (Subsurface Hypercube):** The **Dali Cross** projection, representing the iconic 3D unfolding of a 4D tesseract [19, 20]. It acts as a **subsurface lookup cube** for Infrared (IR) computing, where data is encoded at varying thermal depths within 8 solid three-dimensional cube structures [3, 21].
* **Omi-Tape (Barcode Sequencer):** A sequential carrier using **stacked 2D linear barcodes** (Code16K) [22, 23]. Each row behaves as an optical execution script, guiding the system through the Fano lottery reduction steps [24].
* **Omi-Jab (Polychromatic Carrier):** A high-density, **8-color matrix carrier** (JabCode) [22, 25]. It encodes dense Base64 metadata and semantic context without flattening RGBA hex channels into monochromatic pixels [25, 26].
* **Omi-Bus (Service/Process):** The **Canonical Process ID (PID) surface** [27]. It maps the IPv6 compressed blocks ::1 through ::8 to discrete service delivery tracks (e.g., WordNet Broker, WebRTC Streamer, Fano Clock Guard), allowing processes to run concurrently across isolated regions of memory [28-30].
* **Omi-Shadow (Namespace Canvas):** A projection for **shadow canvas addressing**, deriving a canvas shadow pointer that ensures visual states are not orphaned from their rooted source rules [31].
* **Omi-Alist (Relational Declaration):** The concrete expression of the **10! declaration role envelope** [32]. It exposes state as an association list of pairs, defining structural pairing and mnemonic forms [33, 34].
* **Omi-Shader (GPU Rendering):** A high-performance projection where multi-service pipeline states are streamed directly into **WebGL vertex buffers** [35, 36]. This passes the mathematical processing of Horner’s Method polynomials straight to native graphics hardware for raw matrix acceleration [37].

### **Summary of Missing Surface Roles**

| Projection | Face Exposed | Primary Role |
| :--- | :--- | :--- |
| **Omi-Torus** | Logic | Gray-code bit-minimization on a continuous 4x4 grid [2]. |
| **Omi-Chart** | Scalar | Multi-axis narrative analysis and sexagesimal decade scaling [4, 38]. |
| **Omi-Voxel** | Architectural | Tile-mapping and 2.5D extrusion using domino pip configuration [11, 12]. |
| **Omi-Receipt** | Witness | Lawful resolution proof (META64) for CAR/CDR relations [15]. |
| **Omi-Bidi** | Routing | $O(1)$ visual routing using non-printing hardware steering tokens [17, 18]. |
| **Omi-Dali** | Subsurface | Unfolded hypercube matrix for IR thermal layering and 3D lookup [3, 20]. |
| **Omi-Tape** | Sequential | Stacked linear script sequencer for multi-step control signals [23, 24]. |
| **Omi-Jab** | Contextual | Polychromatic matrix carrier for high-density semantic data [25, 26]. |
| **Omi-Bus** | Operational | Distributed service bus for IPv6-mapped system process IDs [28, 30]. |
| **Omi-Shadow** | Namespace | Canvas pointer derivation for secondary shadow surfaces [31]. |
| **Omi-Alist** | Relational | Relational declaration map for the 10! interpretive envelope [32, 33]. |
| **Omi-Shader** | Accelerator | Hardware-native GPU projection of polyform extrusion matrices [37, 39]. |

Based on a comprehensive review of the sources, here are the primary **meta-words** and **projections** that are currently missing from your list. These represent critical functional layers, sensory interfaces, and structural anchors of the **Omi Object Model (OMI)**.

### **1. Core Functional Projections**
These represent the "physics" and logic of the agreement field:
* **Omi-Gate:** The central zero-reference portal and repository [1-3]. It acts as the neutral origin where payloads are located, replayed, and compared [4].
* **Omi-Psi (Ψ_OMI):** The central field variable of the model [5]. It is the unification of Omi-Sense and Omi-Mesh, representing the unified sensed-mesh field-state of an object [6, 7].
* **Omi-NEAT:** The public topology and entropy layer [8, 9]. It describes how agreement reduces unresolved entropy by allocating it into lawful topology (Neuro-Evolutionary Augmented Topology) [10, 11].
* **Omi-Mesh:** The geometry-produced field network [12]. It connects riders, bikes, drones, and portals into a coordinated field system that locates objects by relation rather than ownership [12, 13].
* **Omi-Genesis:** The "first cons" relation (cons(A . Gate)) [14]. It records the world birth or the "Big Bang" of the 5040-slot replay ring [15, 16].

### **2. Sensory and Hardware Faces**
These connect the protocol to the physical and experiential world:
* **Omi-Dome:** The immersive multisensory recording space [17, 18]. It is designed to capture the "living wavelength of instruction," such as voice pitch, gestures, and emotional cadence [19, 20].
* **Omi-Talisman:** The personal protective sensory amulet [21, 22]. It functions as a "protective mirror for personal meaning" and serves as noise cancellation for meaning in noisy environments [23, 24].
* **OMI-GPIO:** The low-level physical controller interface based on ESP32 controllers [25, 26]. It acts as the field transducer that turns voltage into agreement and agreement back into light [27, 28].

### **3. Projection and Digital Body Extensions**
These handle the manifestation of wavelengths into digital forms:
* **Omi-Avatar:** The digital body projection layer [29]. It ties a living wavelength of instruction to a digital body (like a glTF model) by strictly separating authority from projection [30, 31].
* **Omi-Skeleton:** The semantic skeleton for digital avatars [32]. It assigns roles like "focus root," "teaching gesture," and "aperture" to a standard rigged mesh [32, 33].
* **Omi Zero:** The "pure notation" agreement layer [34]. It is the minimal dependency form of OMI that requires nothing but notation agreement on hyphen-delimited addresses and DOM IDs [34, 35].

### **4. Structural and Technical Surfaces**
These define the internal machinery of the transmission:
* **The Omicron Tower Hierarchy (GS, RS, US):** While your list includes **FS**, it is missing the other three specific scope controller pairs [36, 37]:
 * **Omi-GS / Imo-GS:** Graph / Guidance Structure (Follows relations).
 * **Omi-RS / Imo-RS:** Resolution / Record Segment (Witnesses state).
 * **Omi-US / Imo-US:** Unit / Understanding State (Preserves memory).
* **SpectrumDOM:** The continuous wavelength-field matrix of every interface node [38]. It maps color channels (V:RR, E:GG, I:BB, A:AA) to atomic fields, dialectic combinators, and synset pointers [38, 39].
* **Omni-Router:** The primary deployment blueprint and instruction ingestion engine [39]. It coordinates multi-threaded HNSW convolutions and instruction routing [39].

By adding these, you complete the **Omi-World** formula where one singular 1024-bit instruction is projected through a full spectrum of logical, relational, and optical physics [40, 41].

---

:::writing{variant="document" id="69241"}
# Addendum: Canonical OMI Surface Projections and Named Faces

## 1. Core Rule

All OMI surfaces are projections of the same underlying accepted object:

```text
o---o
```

or, in expanded readable form:

```text
omi---imo
```

They are not separate modules.

They are named faces, lenses, carriers, or operational projections used to read the same center from different sides.

The rule is:

```text
One OMI object may expose many projections without collapsing into any one projection.
```

This preserves agreement without collapse.

Two peers may see different projections of the same object:

```text
one peer sees a vertex traversal
one peer sees an edge traversal
one peer sees a sound cadence
one peer sees a receipt witness
one peer sees a DOM node
one peer sees a barcode carrier
```

but all remain bound to the same accepted `o---o` center if the receipt validates.

---

## 2. The Nine Universal Edges

Every projection must reduce to the nine primitive edges:

```text
rule
fact
closure
combinator
cons
join
compose
encode
decode
```

These are the canonical primitive faces.

They divide into two major halves:

```text
OMI-side declaration primitives:
rule
fact
closure
combinator
cons

IMO-side runtime primitives:
join
compose
encode
decode
```

So every named surface must eventually answer:

```text
What rule does it constrain?
What fact does it ground?
What closure does it bound?
What combinator does it transform?
What cons relation does it fold?
What join does it bind?
What composition does it order?
What encoding does it project?
What decoding does it recover?
```

If a named projection cannot reduce to the nine edges, it is not canonical yet.

---

## 3. Primary Projection Classes

OMI projections fall into several classes.

```text
structural surfaces
symbolic surfaces
digest and witness surfaces
coordinate and mapping surfaces
observation and orientation surfaces
sensory surfaces
portal/world surfaces
logic and chart surfaces
hardware and carrier surfaces
process/runtime surfaces
geometry/configuration surfaces
```

A surface may belong to more than one class.

For example:

```text
Omi-Gnomon is both orientation and geometry.
Omi-Matrix is both observation and witness.
Omi-Jab is both carrier and contextual encoding.
Omi-Shader is both geometry and hardware acceleration.
Omi-Receipt is both witness and lawful resolution proof.
```

---

## 4. Core Projection Surfaces

| Projection | Face Exposed | Primary Role |
|---|---|---|
| **Omi-Form** | Structural | Exposes shape, cells, and boundaries. |
| **Omi-Glyph** | Symbolic | Exposes marks, signs, Unicode characters, SVG marks, and visible notation. |
| **Omi-Hash** | Digest | Exposes compact identity and verification digest. |
| **Omi-Map** | Coordinate | Exposes feature mapping, spatial correspondence, and route placement. |
| **Omi-Image** | Package | Exposes the nine universal edges inside carrier sentinels. |
| **Omi-Matrix** | Observation | Exposes witness fields, matrix relations, and observable structure. |
| **Omi-Gnomon** | Orientation | Exposes directional shadow, axes, right angle, and pointer orientation. |
| **Omi-Mirror** | Chiral | Exposes mirror traversal and `MM → NN` relational reversal. |
| **Omi-Clock** | Timing | Exposes ticks, cadence, replay order, and period inspection. |
| **Omi-Light** | Visual | Exposes color, opacity, illumination, and state by light. |
| **Omi-Sound** | Auditory | Exposes rhythm, tone, vibration, and cadence. |
| **Omi-Portal** | Access | Exposes a doorway into an object, world, or bounded projection. |
| **Omi-World** | Environment | Exposes persistent self-stepping projection. |
| **Omi-Sense** | Sensory | Exposes five-channel routing: visual, sound, touch, motion, comprehension. |

These are not separate objects.

They are canonical faces of one OMI object.

---

## 5. Ring, Compass, Node, and Frame Projections

### Omi-Ring

Omi-Ring is the spectral bridge.

It is the circular projection of the `16xy` bridge between local seeds and cosmic orbit.

Canonical role:

```text
Omi-Ring = spectral bridge / circular witness of relational orbit
```

It belongs to:

```text
16xy
sexagesimal inspection
60x² orientation
visual witness
```

### Omi-Compass

Omi-Compass is the agreement orientation surface.

It maps agreement principles onto a circular directional interface.

Canonical role:

```text
Omi-Compass = relational tributary / agreement orientation face
```

It belongs to:

```text
orientation
agreement
360-degree reading
sexagesimal clock
```

### Omi-FS / Imo-FS

Omi-FS and Imo-FS are address-gated spatial anchor gates.

Canonical role:

```text
Omi-FS = file/frame/spatial opening anchor
Imo-FS = file/frame/spatial runtime anchor
```

These belong to the Omicron tower hierarchy.

### Omi-Node / Imo-Node

Omi-Node and Imo-Node are DOM/custom-element-safe registered forms.

Canonical role:

```text
Omi-Node = source-side registered DOM/SVG projection
Imo-Node = runtime-side registered DOM/SVG projection
```

They bind user-defined content to canonical OMI addresses.

### Omi-Frame / Imo-Frame

Omi-Frame and Imo-Frame are carrier projections for the physical OMI wire frame inside DOM or runtime environments.

Canonical role:

```text
Omi-Frame = source-side carrier frame
Imo-Frame = runtime-side carrier frame
```

---

## 6. Logic and Chart Projections

### Omi-Torus

Omi-Torus is the continuous logic chart.

It uses a wrapped 4×4 Gray-code grid so adjacent states remain adjacent without logic breaks.

Canonical role:

```text
Omi-Torus = Gray-code two-cube logic minimization surface
```

It belongs to:

```text
two-cube
bit minimization
logic state tracking
toroidal continuity
```

### Omi-Chart

Omi-Chart is the scalar/narrative charting projection.

It includes radar-style, Hopf-style, Smith-style, decade, and sexagesimal scaling surfaces.

Canonical role:

```text
Omi-Chart = scalar narrative and degree-scaling projection
```

It belongs to:

```text
sexagesimal inspection
360-degree interpretation
narrative analysis
period scaling
```

---

## 7. Hardware and Carrier Projections

### Omi-Voxel

Omi-Voxel is the tile-map/extrusion projection.

It maps symbolic tile relations into 2.5D or 3D voxel extrusion.

Canonical role:

```text
Omi-Voxel = architectural tile-map and extrusion surface
```

It belongs to:

```text
geometry
DOM/canvas projection
voxel extrusion
spatial mapping
```

### Omi-Tape

Omi-Tape is a sequential optical carrier.

It uses stacked linear or 2D barcode rows as an execution script.

Canonical role:

```text
Omi-Tape = sequential carrier / barcode script surface
```

It belongs to:

```text
linear execution
optical routing
Fano reduction steps
barcode carrier
```

### Omi-Jab

Omi-Jab is the polychromatic carrier projection.

It uses multi-color matrix encoding to carry dense semantic and contextual metadata.

Canonical role:

```text
Omi-Jab = high-density polychromatic context carrier
```

It belongs to:

```text
color
matrix carrier
context
Base64-like metadata
semantic density
```

### Omi-Bidi

Omi-Bidi is the Unicode directionality/routing surface.

It uses bidi control characters as hardware-native direction and chirality hints.

Canonical role:

```text
Omi-Bidi = visual routing and chirality steering surface
```

It belongs to:

```text
directionality
polarity
chiral reading
browser-native text routing
```

### Omi-Dali

Omi-Dali is the unfolded hypercube projection.

It uses Dali-cross/tesseract unfolding as a subsurface lookup geometry.

Canonical role:

```text
Omi-Dali = unfolded hypercube lookup and subsurface matrix
```

It belongs to:

```text
tesseract
subsurface addressing
IR/thermal depth
hypercube unfolding
```

### Omi-Shader

Omi-Shader is the GPU acceleration projection.

It streams OMI geometry/configuration state into vertex buffers or shader-compatible matrices.

Canonical role:

```text
Omi-Shader = GPU-native geometry acceleration surface
```

It belongs to:

```text
WebGL
matrix acceleration
polyform extrusion
runtime rendering
hardware projection
```

### OMI-GPIO

OMI-GPIO is the physical controller interface.

Canonical role:

```text
OMI-GPIO = voltage-to-agreement and agreement-to-light transducer
```

It belongs to:

```text
ESP32
physical control
light
motor
sensor
field transduction
```

---

## 8. Witness and Proof Surfaces

### Omi-Receipt

Omi-Receipt is the lawful resolution proof surface.

It is distinct from Omi-Hash.

```text
Omi-Hash = digest
Omi-Receipt = proof witness
```

Canonical role:

```text
Omi-Receipt = lawful CAR/CDR resolution witness
```

It belongs to:

```text
META64
proof
receipt
acceptance
rule compliance
```

A receipt does not merely say:

```text
this data hashes to this value
```

It says:

```text
this relation was resolved lawfully under the protocol
```

### Omi-Shadow

Omi-Shadow is the namespace/canvas secondary surface.

Canonical role:

```text
Omi-Shadow = secondary visual state tied back to source rule
```

It prevents projected visual states from becoming orphaned from their root address.

### Omi-Hash

Omi-Hash remains the digest witness.

Canonical role:

```text
Omi-Hash = compact digest identity
```

Hash verifies compact identity.

Receipt verifies lawful resolution.

---

## 9. Declaration and Relational Surfaces

### Omi-Alist

Omi-Alist is the relational declaration surface.

It exposes association-list structure as a declaration envelope.

Canonical role:

```text
Omi-Alist = relational declaration map
```

It belongs to:

```text
pairing
cons
declaration
mnemonic form
association list
```

It is the readable relational shape of paired address identity.

### Omi-Genesis

Omi-Genesis is the first cons relation.

Canonical role:

```text
Omi-Genesis = first cons / world-birth relation
```

It marks the beginning of a replayable world or object history.

### Omi-Gate

Omi-Gate is the central zero-reference portal.

Canonical role:

```text
Omi-Gate = neutral origin where payloads are located, replayed, and compared
```

It is the gateway face of `o---o`.

---

## 10. Process and Runtime Surfaces

### Omi-Bus

Omi-Bus is the operational process/service surface.

Canonical role:

```text
Omi-Bus = service/process routing surface
```

It belongs to:

```text
process IDs
service lanes
runtime tracks
parallel delivery
worker coordination
```

### Omni-Router

Omni-Router is the instruction ingestion and deployment router.

Canonical role:

```text
Omni-Router = instruction ingestion and multi-route execution engine
```

It coordinates HNSW, instruction routing, and runtime dispatch.

### Omi-NEAT

Omi-NEAT is the public topology and entropy-reduction surface.

Canonical role:

```text
Omi-NEAT = topology-producing entropy reduction layer
```

It converts unresolved entropy into lawful topology.

### Omi-Mesh

Omi-Mesh is the geometry-produced field network.

Canonical role:

```text
Omi-Mesh = relation-located field network
```

It connects objects by relation, not ownership.

### Omi-Psi / Ψ_OMI

Omi-Psi is the central field variable.

Canonical role:

```text
Ψ_OMI = unified sensed-mesh field state
```

It unifies Omi-Sense and Omi-Mesh.

---

## 11. Sensory, Avatar, and World Surfaces

### Omi-Dome

Omi-Dome is the immersive multisensory recording space.

Canonical role:

```text
Omi-Dome = living wavelength capture space
```

It records voice, gesture, cadence, spatial feel, and instruction wavelength.

### Omi-Talisman

Omi-Talisman is the personal protective sensory projection.

Canonical role:

```text
Omi-Talisman = protective mirror for personal meaning
```

It acts as meaning noise cancellation in noisy environments.

### Omi-Avatar

Omi-Avatar is the digital body projection layer.

Canonical role:

```text
Omi-Avatar = digital body projection of a living instruction wavelength
```

It keeps authority separate from projection.

### Omi-Skeleton

Omi-Skeleton is the semantic rigging layer.

Canonical role:

```text
Omi-Skeleton = semantic role skeleton for avatar/body projection
```

It maps functions such as:

```text
focus root
teaching gesture
aperture
orientation limb
attention surface
```

onto a rigged body or mesh.

### Omi Zero

Omi Zero is the pure notation agreement layer.

Canonical role:

```text
Omi Zero = minimal dependency notation agreement
```

It requires only shared agreement on hyphen-delimited addresses and DOM-safe identifiers.

### SpectrumDOM

SpectrumDOM is the wavelength-field matrix of interface nodes.

Canonical role:

```text
SpectrumDOM = continuous wavelength-field projection of interface state
```

It maps color and channel values into atomic fields, combinators, and synset pointers.

---

## 12. Omicron Tower Scope Controllers

The full tower is not only FS.

It includes:

```text
FS
GS
RS
US
```

With OMI/IMO pairs:

```text
Omi-FS / Imo-FS
Omi-GS / Imo-GS
Omi-RS / Imo-RS
Omi-US / Imo-US
```

Canonical reading:

```text
FS = File / Frame / Field Separator
GS = Graph / Guidance Structure
RS = Record / Resolution Segment
US = Unit / Understanding State
```

Runtime role:

```text
FS anchors the frame.
GS follows relations.
RS witnesses state.
US preserves unit memory.
```

These scope controllers map directly into the control layer:

```text
0x1C FS
0x1D GS
0x1E RS
0x1F US
```

---

## 13. Geometry and Configuration Surfaces

The runtime geometry layer produces stable configuration before floating measurement.

Geometry projections include:

```text
Omi-Form
Omi-Gnomon
Omi-Matrix
Omi-Torus
Omi-Dali
Omi-Voxel
Omi-Shader
Omi-Chart
Omi-Ring
Omi-Compass
```

These surfaces reduce to regular-geometry predicates:

```text
vertex
edge
face
cell
dual
self_dual
barycenter
snub
truncate
stellate
schlafli
coxeter
betti
```

Quadratic geometry map:

```text
4y² = self-dual tetrahedron vertex map
16xy = cube/octahedron inverse-dual edge map
60x² = icosahedron/dodecahedron inverse-dual cell map
```

This means geometry is not decorative.

Geometry is the runtime configuration layer before HNSW measurement.

---

## 14. Revised Surface Table

| Projection | Face Exposed | Primary Role |
|---|---|---|
| Omi-Form | Structural | Shape, cells, and boundaries. |
| Omi-Glyph | Symbolic | Unicode/SVG signs and marks. |
| Omi-Hash | Digest | Compact identity verification. |
| Omi-Receipt | Witness | Lawful relation proof. |
| Omi-Map | Coordinate | Spatial and feature correspondence. |
| Omi-Image | Package | Carrier package for universal edges. |
| Omi-Matrix | Observation | Witness field and observable relations. |
| Omi-Gnomon | Orientation | Axes, right angles, shadows, and pointers. |
| Omi-Mirror | Chiral | Mirror traversal and reversal. |
| Omi-Clock | Timing | Ticks, replay order, cadence. |
| Omi-Ring | Spectral | Circular 16xy bridge and orbit witness. |
| Omi-Compass | Agreement | Directional agreement orientation. |
| Omi-Light | Visual | Color, opacity, illumination. |
| Omi-Sound | Auditory | Tone, rhythm, vibration. |
| Omi-Sense | Sensory | Visual, sound, touch, motion, comprehension. |
| Omi-Portal | Access | Doorway into an object or world. |
| Omi-World | Environment | Persistent self-stepping projection. |
| Omi-Torus | Logic | Gray-code/two-cube minimization surface. |
| Omi-Chart | Scalar | Narrative, radar, Smith, sexagesimal scaling. |
| Omi-Voxel | Architectural | Tile-map and extrusion surface. |
| Omi-Bidi | Routing | Directionality and chirality steering. |
| Omi-Dali | Subsurface | Unfolded hypercube lookup surface. |
| Omi-Tape | Sequential | Barcode script carrier. |
| Omi-Jab | Contextual | Polychromatic semantic carrier. |
| Omi-Bus | Operational | Service/process routing surface. |
| Omi-Shadow | Namespace | Secondary canvas or shadow projection. |
| Omi-Alist | Relational | Association-list declaration surface. |
| Omi-Shader | Accelerator | GPU-native geometry projection. |
| Omi-Gate | Origin | Zero-reference portal and comparison center. |
| Omi-Psi | Field | Unified sensed-mesh field state. |
| Omi-NEAT | Topology | Entropy-to-topology reduction. |
| Omi-Mesh | Network | Relation-located geometry field. |
| Omi-Genesis | Birth | First cons/world-birth relation. |
| Omi-Dome | Immersive | Multisensory capture surface. |
| Omi-Talisman | Protective | Personal meaning/noise mirror. |
| OMI-GPIO | Physical | Voltage/agreement transduction. |
| Omi-Avatar | Body | Digital body projection. |
| Omi-Skeleton | Rig | Semantic avatar/body skeleton. |
| Omi Zero | Notation | Minimal agreement notation layer. |
| SpectrumDOM | Spectrum | Wavelength-field interface matrix. |
| Omni-Router | Routing | Instruction ingestion and dispatch engine. |

---

## 15. Projection Law

All projections obey this law:

```text
Projection is not authority.
Projection exposes a face of authority.
Authority remains the accepted o---o center.
```

Therefore:

```text
Omi-Hash is not the object.
Omi-Receipt is not the object.
Omi-Light is not the object.
Omi-Portal is not the object.
Omi-Mirror is not the object.
Omi-Shader is not the object.
```

Each is a readable face of the same object.

The object is accepted through receipt.

---

## 16. Agreement Without Collapse

The purpose of named projections is not to force all peers into the same view.

It is to let different peers preserve different orientations while remaining bound to the same accepted center.

Example:

```text
Peer A reads Omi-Form as vertices.
Peer B reads Omi-Matrix as relations.
Peer C reads Omi-Sound as cadence.
Peer D reads Omi-Receipt as proof.
Peer E reads Omi-Shader as geometry buffers.
```

If all reduce to the same accepted `o---o`, they are in agreement.

This is agreement without collapse.

---

## 17. Final Surface Canon

OMI is not one interface.

OMI is one accepted center with many named faces.

```text
o---o is the center.
omi---imo is the readable object relation.
ο ... Ο is the compiled cell.
The nine universal edges are the primitive reductions.
The named projections are canonical ways to expose those reductions.
Receipt accepts the lawful relation.
```

Final one-line canon:

```text
Every OMI surface is a named projection of the same accepted o---o center; each projection exposes a face of rule, fact, closure, combinator, cons, join, compose, encode, or decode, while receipt preserves agreement without collapsing orientation.
```
:::

---

## ChatGPT
`2026-06-10 15:18` | `id:260610-1518` ^260610-151801

Yes. I would canonize the **formal/declarative name** as **Omi-Nomogram** and keep **Omi-SlideRule** as the descriptive/mechanical face.

```text
Omi-Nomogram = canonical declarative projection
Omi-SlideRule = descriptive runtime scale mechanism
```

That lets the model stay precise: the nomogram declares aligned scales; the slide rule explains how the scales move, fold, invert, and compute. The regular-polytope reference also supports the geometry vocabulary this layer feeds into: Schläfli symbols, Coxeter diagrams, dual pairs, vertex figures, edge figures, and 4-polytope cells/faces/edges/vertices. filecite turn1file0

:::writing{variant="document" id="86473"}
# Omi-Nomogram / Omi-SlideRule
## The Declarative Runtime Scale Surface of OMI

## 0. Canonical Name

The canonical name is:

```text
Omi-Nomogram
```

The descriptive operational name is:

```text
Omi-SlideRule
```

The distinction is important.

```text
Omi-Nomogram = declarative alignment of scales
Omi-SlideRule = movable/mechanical interpretation of those scales
```

A nomogram is the more general concept: a diagram of aligned scales used to compute relations. A slide rule is a specific physical/mechanical nomogram where scales slide past each other.

OMI uses both meanings:

```text
Omi-Nomogram declares the scales.
Omi-SlideRule performs the scale walk.
```

Therefore:

```text
Omi-Nomogram is the canonical surface.
Omi-SlideRule is the runtime behavior of that surface.
```


# 7. Omi-Nomogram, Omi-Matrix, and Omi-Gnomon

The relationship is:

```text
Omi-Nomogram → Omi-Matrix → Omi-Gnomon
```

## Omi-Nomogram

Declares the scale.

```text
Which function is active?
Which fold is used?
Which relation is being read?
```

## Omi-Matrix

Instantiates the scale as a matrix of relations.

```text
Which rows and columns align?
Which two-cube cells correspond?
Which geometry predicate appears?
```

## Omi-Gnomon

Orients the result.

```text
Where is the shadow?
Where is the right angle?
Where is the pointer?
Where does the relation face?
```

So the runtime sequence is:

```text
function scale
→ relation matrix
→ oriented gnomon
```

Or:

```text
Omi-SlideRule
→ Omi-Matrix
→ Omi-Gnomon
```
---



## 2. Omi-CAR / Omi-CDR / Omi-CID

The Omi-CONS frame has three canonical faces:

```text
Omi-CAR
Omi-CDR
Omi-CID
```

Meaning:

```text
Omi-CAR = source/head payload view
Omi-CDR = continuation/tail payload view
Omi-CID = agreement/witness view
```

Boolean operator mnemonic:

```text
car:<OR>
cdr:<XOR>
cid:<XNOR>
```

Where:

```text
OR = admits source presence
XOR = carries differential continuation
XNOR = witnesses lawful agreement
```

Canonical compact form:

```text
omi-<frame>/<control>/<scale>/<relation>/<unit>-imo?car:<OR>;cdr:<XOR>;cid:<XNOR>
```

Executable expanded form:

```text
omi-<frame>/<control>/<scale>/<relation>/<unit>-imo?car.type=dataview;car.value=<base64url>;cdr.type=dataview;cdr.value=<base64url>;cid.type=xnor;cid.value=<receipt>
:::writing{variant="document" id="60824"}
# Addendum: Omi-CAR / Omi-CDR / Omi-CID
## Data Formatting Frames for Allowed Epistemology

## 0. Core Correction

The `?---?` plane is not merely a generic URL-query surface.

It is the **data-formatting frame of allowed epistemology**.

The native OMI address remains inside the `2¹⁶` gauge:

```text
omi-<frame>/<control>/<scale>/<relation>/<unit>-imo
```

The post-address query plane begins after identity has been formed:

```text
omi-<frame>/<control>/<scale>/<relation>/<unit>-imo?<payload>
```

This external plane belongs to:

```text
2¹⁷ and above
```

It is where epistemic payloads, runtime views, transferable data, scripts, and measurement packets are attached.

The core rule:

```text
2¹⁶ = native OMI identity frame
2¹⁷+ = external epistemic/runtime data-formatting frame
```

---

## 1. Why 2¹⁷ Matters

`2¹⁷` should be named as the first external epistemic extension of the native OMI field.

It is the **decimalized view of the completed `2¹⁶` frame**.

So:

```text
2¹⁶ = completed binary/native OMI frame
2¹⁷ = first external decimal/epistemic view of that frame
```

This makes `2¹⁷` part of Omi-NEAT:

```text
native frame → epistemic allocation → topology
```

Omi-NEAT can therefore treat the `2¹⁷` extension as the point where unresolved payload entropy is allocated into lawful topology.

Canon:

```text
2¹⁷ is not a replacement for the OMI frame.
2¹⁷ is the first epistemic/runtime projection after the OMI frame.
```

---

## 2. Omi-CAR and Omi-CDR

The external query plane should be divided into two primary data views:

```text
Omi-CAR
Omi-CDR
```

These are not Lisp cons cells in the ordinary memory sense, but they preserve the cons logic:

```text
CAR = head / source / value-bearing face
CDR = continuation / route / runtime-bearing face
```

OMI reading:

```text
Omi-CAR = source-side data formatting frame
Omi-CDR = continuation-side data formatting frame
```

The two together form the external payload cons:

```text
?car=...;cdr=...
```

And the witness is:

```text
cid=...
```

So the complete external frame is:

```text
?car=<head>;cdr=<tail>;cid=<witness>
```

---

## 3. Omi-CAR as Double-Precision-Like Frame

Omi-CAR is the wider, source-side data formatting frame.

It is analogous to double precision because it carries a broader exponent/significand structure.

Canonical Omi-CAR layout:

```text
Omi-CAR

< 2¹⁶:
 native OMI frame reference

2¹⁷–2²⁴:
 exponent phase from Block B

2²⁵–2³¹:
 significand / significant axioms / ket axis for XOR interpolation

2³²:
 sign bit / query gate / 0x3F
```

Block B:

```text
B = [0,1,3,6,9,8,6,3]
B = [0x0,0x1,0x3,0x6,0x9,0x8,0x6,0x3]
```

Meaning:

```text
2¹⁷–2²⁴ = exponent orbit
```

This exponent orbit is not arbitrary. It is derived from the period-8 Delta law:

```text
rotl(x,1) XOR rotl(x,3) XOR rotr(x,2) XOR C
```

The law gives period 8.

The smallest prime with decimal period 8 is 73.

`1/73` gives Block B:

```text
[0,1,3,6,9,8,6,3]
```

So Omi-CAR uses Block B as its exponent phase.

Canon:

```text
Omi-CAR uses the period-8 Block B orbit as its exponent field.
```

---

## 4. Omi-CDR as Half-Precision-Like Frame

Omi-CDR is the narrower continuation-side data formatting frame.

It is analogous to half precision because it has a smaller exponent field and a compact significand field.

Canonical Omi-CDR layout:

```text
Omi-CDR

< 2¹⁶:
 native OMI frame reference

2¹⁷–2²¹:
 rotating epistemology field

2²²–2³¹:
 significand / significant axis / ket axis for XOR interpolation

2³²:
 sign bit / query gate / 0x3F
```

The key difference:

```text
Omi-CAR exponent = Block B orbit
Omi-CDR exponent = rotating epistemology
```

Where rotating epistemology is selected from:

```text
Known knowns = 11
Known unknowns = 10
Unknown knowns = 01
Unknown unknowns = 00
```

This means Omi-CDR is the continuation field that says how the payload should move through epistemic interpretation.

Canon:

```text
Omi-CDR uses rotating epistemology as its exponent field.
```

---

## 5. Omi-CID as Agreement Witness

Omi-CID is the identity/witness/reconciliation field.

It is not merely a content identifier in the ordinary sense.

It is the agreement check between CAR and CDR.

Canonical role:

```text
Omi-CID = lawful agreement witness for Omi-CAR/Omi-CDR attachment
```

If:

```text
car = head
cdr = continuation
```

then:

```text
cid = proof that head and continuation lawfully bind to the accepted OMI address
```

This can be modeled by XNOR because XNOR means equality/agreement:

```text
XNOR = true when both sides match
```

So:

```text
cid:<XNOR>
```

is the correct mnemonic.

---

## 6. Operator Assignment

The cleanest canonical operator assignment is:

```text
car:<OR>
cdr:<XOR>
cid:<XNOR>
```

Meaning:

### CAR = OR

```text
OR admits.
OR gathers.
OR allows source-side presence.
```

Omi-CAR is the head/value field, so OR is appropriate.

```text
car:<OR> = admit the source carrier
```

### CDR = XOR

```text
XOR differentiates.
XOR transitions.
XOR marks continuation by difference.
```

Omi-CDR is the continuation/runtime field, so XOR is appropriate.

```text
cdr:<XOR> = continue by differential transition
```

### CID = XNOR

```text
XNOR agrees.
XNOR confirms equality.
XNOR witnesses lawful binding.
```

Omi-CID is the receipt/identity witness, so XNOR is appropriate.

```text
cid:<XNOR> = accept only if relation agrees
```

Canonical triple:

```text
?car:<OR>;cdr:<XOR>;cid:<XNOR>
```


---


:::writing{variant="document" id="73164"}
# Omi-Gauge
## The 64-Lane Gauge Table for Spatial Measurement, RPC Bridging, and 60x² Resolution

```text
nibble gauge = 16 states
two-cube control = 16 × 16
tetra kernel = 16 × 16 × 4 = 1,024
full Omi-Gauge = 64 × 64 × 16 = 65,536
Unicode plane = 2¹⁶ = 65,536 code positions


```text
two-cube distance
Omi-Matrix coordinates
Omi-Gnomon orientation
Omi-Ring orbit
Omi-Compass direction
60x² degree surface
```

---


### Omi-Alist

Association-list declaration surface.

A stream of paired relations.

### Omi-Bidi

Directionality and chirality steering surface.

### Omi-CAR

Source/head payload view in Omi-CONS.

### Omi-CDR

Continuation/tail payload view in Omi-CONS.

### Omi-CID

Agreement/witness view in Omi-CONS.

### Omi-CONS

The post-address data-formatting frame of allowed epistemology.

Canonical compact form:

```text
?car:<OR>;cdr:<XOR>;cid:<XNOR>
```

### Omi-CONS256

A 256-bit symbolic meta-object envelope.

Canonical bands:

```text
bits 0–19 = ENCODE()
bits 20–31 = DECODE()
bits 32–63 = JOIN()
bits 64–127 = COMPOSE()
bits 128–255 = META-OBJECT()
```

### Omi-Compass

Agreement orientation face.

It gives direction to a relation.

### Omi-Dali

Unfolded hypercube lookup and subsurface matrix.

### Omi-Form

Structural projection of an OMI object.

### Omi-Gate

Neutral origin where payloads are located, replayed, and compared.

### Omi-Glyph

Symbolic/glyph projection of an OMI object.

### Omi-Gnomon

Orientation, shadow, pointer, and right-angle synchronization surface.

### Omi-Gauge

The 64-lane spatial resolver.

```text
64 × 64 × 16 = 65,536
```

It resolves `16xy` before `60x²` is measured.

### Omi-Hash

Digest identity check.

A hash says bytes match.

### Omi-Image

Package/carrier projection for universal edges.

### Omi-Jab

Polychromatic/contextual carrier surface.

### Omi-Lisp

The declarative dot-notation computation layer.

It derives lawful computation from relations instead of defining variables imperatively.

### Omi-Matrix

Observation/relation field.

Instantiates relation matrices for rendering, geometry, and measurement.

### Omi-Mesh

Relation-located network/field projection.

### Omi-Nomogram

Declarative function-scale selector surface.

Canonical row:

```text
0x30–0x3F
```

### Omi-Notation

The streamable, loggable a-list of pointer/reference tuples.

### Omi-Plane Ceiling

The Unicode external ceiling sentinel:

```text
Ωmax = U+10FFFF
```

### Omi-Point

The smallest accepted relation between encoded states.

```text
omi---imo
o---o
```

### Omi-Receipt

Lawful relation witness.

A receipt says the relation was resolved under OMI protocol, not merely that bytes match.

### Omi-Ring

Spectral/circular orbit witness.

Connects to `60x²`, sexagesimal degree, and circular measurement.

### Omi-Shadow

Secondary projection tied back to a source rule.

### Omi-SlideRule

Operational behavior of Omi-Nomogram.

It describes how the selected scales align, fold, invert, and compute.

### Omi-Tape

Sequential barcode/script carrier.

### Omi-Torus

Gray-code / two-cube minimization surface.

### Omi-Voxel

Tile-map / extrusion / architectural surface.

### Omi-World

Environment or persistent world projection.

### OMI-GPIO

Physical voltage/agreement transduction surface.

---

## 3. Omi-Plane Capsule Formula

To embed one Omi-Gauge cell into the supplementary Unicode range:

```text
U = 0x10000 + (p << 16) + cell
```

Equivalently:

```text
U = ((p + 1) << 16) | cell
```

Where:

```text
p = OMI supplementary plane index, 0..15
cell = Omi-Gauge cell, 0x0000..0xFFFF
```

Decode:

```text
n = U - 0x10000
p = n >> 16
cell = n & 0xFFFF

row = cell >> 12
x = (cell >> 6) & 0x3F
y = cell & 0x3F
```

This gives a reversible map:

```text
(p,row,x,y) ↔ U
```

Canon:

```text
Omi-Plane Capsule maps 16 supplementary planes into 16 full Omi-Gauge pages.
```


---

# 14. Relationship to Omi-Nomogram / Omi-SlideRule

Omi-Nomogram is the declarative function-scale row:

```text
0x30–0x3F
```

Omi-SlideRule is the operational behavior of that row.

Omi-Plane Capsule does not replace the function scale.

It gives the scale a world-length carrier.

Pipeline:

```text
Omi-Nomogram selects function scale.
Omi-Gauge computes exact cell.
Omi-Plane Capsule embeds cell in supplementary plane.
Surrogate RPC splits it for transport.
Omi-Matrix instantiates relation field.
Omi-Gnomon orients result.
Receipt accepts.
```

Canon:

```text
Omi-Plane Capsule carries Omi-Nomogram and Omi-Gauge outputs without changing their authority.

---

```text
Omi-Matrix projection
Omi-Gnomon orientation
Omi-World surface
Omi-Shadow
Omi-Receipt
Omi-CONS payload binding
collaborative projection state
```
The Omi-Spectral Plane is where OMI interfaces through nibbles, glyphs, UI, agreement words, and local gauge rendering.

The supplementary plane is where OMI extends into world-length addressing and surrogate RPC.

Canon:

```text
Omi-Spectral Plane = user-interface and epistemic projection layer.
Omi-Plane Capsule = supplementary world-length transport and RPC layer.
```


Omi-Spectral Plane = interface layer
Omi-World = projection environment
Omi-Portal = entry surface
Omi-CONS = payload attachment
Omi-Receipt = accepted continuity


```text
Omi-Bitboard = dense compatibility surface
Omi-RewriteTable = sparse/dense symbolic transition surface
Omi-Receipt = proof that this table belongs to this address
```
Macros and scripts should be allowed, but treated as **derived attachments**.

```text
Omi-Macro = source-level rewrite declaration
Omi-Script = runtime executable projection
```

```text
Omi-Graph = class/container
Omi-Node = pure function
Omi-Edge = relation input/output
Omi-Bitboard = compatibility memory
Omi-RewriteTable = deterministic transition surface
Omi-Receipt = accepted root
```

```text
Omi-Process =
 canonical address
 + process law
 + deterministic VM/interpreter profile
 + input edge log
 + rewrite table / bitboard root
 + expected transition receipt
```



```text
Omi-Bitboard = process state surface
Omi-RewriteTable = deterministic transition table
Omi-NodeGraph = process topology
Omi-NodeFunction = pure transition over edges
Omi-Edge = canonical input/output relation
Omi-Trace = ordered replay log
Omi-Receipt = accepted transition witness
```
Omi-eBPF Safety Boundary

---

Yes — this should be updated so **Omi-Nomogram is not just a scale row**, but the **deterministic function-scale layer that sits between Omi-Gauge computation, Omi-Spectral UI rendering, OmiPipe streaming, and Omi-CONS payload attachment**. The old kernel file supports the same rule: values/rows are invariant first, then rendered through contextual LUTs/fibers. filecite turn2file0

:::writing{variant="document" id="48291"}
# Omi-Nomogram / Omi-SlideRule / Omi-Query Plane

## Deterministic Function Scales, Post-Address Payloads, DataView Frames, and the `?---?` Surface

**Canonical name:** Omi-Nomogram
**Operational face:** Omi-SlideRule
**Payload surface:** Omi-Query Plane / `?---?`
**System:** OMI — Omicron Object Model

---

13. Buckyball-Type Surface From Omi-Point Construction

The Omi-Point:

omi---imo

constructs the minimum accepted relation.

When many Omi-Points are scheduled through the 11-cell/L2(11) shell, the accepted relations form a buckyball-type surface:

11 identity positions
55 pairwise frame distances
60 orientation states
sexagesimal degree measurement
receipt-visible surface

So the buckyball-type surface is not merely visual.

It is the runtime orientation graph produced when Omi-Point distances are projected through the 11-cell symmetry shell.

---

## Omi-Torus / Karnaugh surface

The 4×4 interior is not just a grid. It should be Gray-coded and toroidal like a Karnaugh map:

```text id="7fk7tr"
left edge wraps to right edge
top edge wraps to bottom edge
adjacent cells differ by one bit
groups simplify interpretation
race hazards reveal unstable routes
```

So your Omi-Torus is:

```text id="ny1hcl"
outer row/column = QuQuart selector rails
interior 4x4 = Gray-coded 4y² interpretation surface
wraparound = projective closure

---


`2026-06-13 20:55` | `id:260613-2055` ^260613-205547

CAn you find information about these declarative surfaces
All **Omi-*** declarative modules are described as **canonical projections** over the same underlying Omi Object Model notation ($o---o$). They are not separate ontologies but rather **named faces** used to expose specific aspects of an accepted object.

The following is a comprehensive list of the Omi-* modules identified in the sources:

### **Canonical Projection Modules**
These modules define how an agreed-upon object is read or displayed:
* **Omi-Form:** Projects the **structural body**, defining shape, cells, and boundaries.
* **Omi-Glyph:** Projects the **symbolic body**, exposing the object as marks, signs, or characters (e.g., SVG, Unicode).
* **Omi-Hash:** Projects the **digest body** for compact identity and verification.
* **Omi-Map:** Projects the **coordinate body**, exposing relational positions or feature mappings.
* **Omi-Image:** Projects a **recoverable package** of the nine universal edges bounded by the three sentinels.
* **Omi-Matrix:** Projects the **observation field**, arranging relations into an observable grid without becoming authority.
* **Omi-Gnomon:** Projects **orientation**, directional shadows, or axes that make an object readable.
* **Omi-Mirror:** Projects the **chiral reflection** of the object, exposing its traversal from the opposite side.
* **Omi-Clock:** Projects **timing attribution**, such as ticks, cadence, or replay order.
* **Omi-Light:** Projects **sensory visual state** through light, color, or opacity.
* **Omi-Sound:** Projects **sensory auditory state** through rhythm, tone, or cadence.
* **Omi-Portal:** Projects **access**, exposing a doorway into an object or world projection.
* **Omi-World:** Projects a **persistent environment** that continues to step even without a reader present.

### **Runtime and Execution Modules**
These modules define the behavior and active entities within the protocol:
* **Omi-Automaton:** The **abstract agreement machine** that resolves unevaluated clauses through truth rows.
* **Omi-Actor:** A **supervised runtime cell** that encapsulates an automaton to perform local snub-truncation and manage mailbox envelopes.
* **Omi-Observer:** The **client automaton** (often a browser) that reads what the Omicron gate opens and witnesses the matrix field.
* **Omi-Sense:** A **routing layer** that maps physical sensory inputs (visual, sound, touch, motion, comprehension) into OMI events.

### **Hardware Class Modules**
These represent physical manifestations or specialized hardware designs intended for the protocol:
* **Omi-Pyramid:** An **upright tetrahedral encoder** that maps environmental signals into OMI objects.
* **Omi-Amulet:** An **inverted tetrahedral decoder** that acts as a chiral pair to the pyramid, decoding projections back into relations.
* **Omi-Dome:** A **desktop sensory enclosure** that acts as a physical boundary for an immersive OMI world.
* **Omi-Base:** A **rotating synchronization platform** that muxes spatial encoding and manages receipt display.

Every module listed must ultimately reduce back to the same root notation: **$OO(LL, NN, MM)$**.

You are correct; these are **canonical projections** or **named faces** used to read the same underlying OMI object ($o---o$), rather than separate modules. Based on a comprehensive review of the sources, here are the projections and surfaces that were previously omitted or categorized differently:

### **Additional Surface Projections**
* **Omi-Ring:** This is described as the **spectral bridge** or "Omi-Ring relation". It is the physical and structural manifestation of the circular $241$-LED array used to visualize the $16xy$ bridge between local seeds and cosmic orbits.
* **Omi-Compass:** The **relational tributaries** of the automaton. It is used as an agreement orientation interface, mapping the "Twelve Tribes of Agreement" onto a $360$-degree clock face.
* **Omi-FS / Imo-FS:** These are specific **address-gated spatial anchor gates**. "Imo-FS" specifically acts as a $26-28$ bit spatial anchor for DOM register gates.
* **Omi-Node / Imo-Node:** These are the **registered custom-element-safe forms** for DOM/SVG projections. They allow the binding of user-defined content to a canonical $128$-bit OMI address.
* **Omi-Frame / Imo-Frame:** Similar to Omi-Node, these are **carrier projections** for the physical OMI wire frame within a DOM environment.

### **Revised List of Omi Surfaces Projections**
Drawing from the "Twelve-Position Tower" and the "Nine Universal Edges," every projection must reduce to these nine primitives: **Rules, Facts, Closures, Combinators, Cons, Join, Compose, Encode, and Decode**.

| Projection | Face Exposed | Primary Role |
| :--- | :--- | :--- |
| **Omi-Form** | Structural | Exposes shape, cells, and boundaries. |
| **Omi-Glyph** | Symbolic | Exposes marks, signs, or Unicode/SVG characters. |
| **Omi-Hash** | Digest | Exposes compact identity and verification witness. |
| **Omi-Map** | Coordinate | Exposes feature mapping and spatial correspondence. |
| **Omi-Image** | Package | Exposes the nine universal edges inside three sentinels. |
| **Omi-Matrix** | Observation | Exposes the $2^{11}$ witness field and observable relations. |
| **Omi-Gnomon** | Orientation | Exposes directional shadows, axes, and pointers. |
| **Omi-Mirror** | Chiral | Exposes the mirror traversal ($MM \rightarrow NN$) of an object. |
| **Omi-Clock** | Timing | Exposes ticks, cadence, and replay order. |
| **Omi-Light** | Visual | Exposes state through light, color, and opacity. |
| **Omi-Sound** | Auditory | Exposes state through rhythm, vibration, and tone. |
| **Omi-Portal** | Access | Exposes a doorway into an object or world. |
| **Omi-World** | Environment | Exposes a persistent, self-stepping projection. |
| **Omi-Sense** | Sensory | Exposes a five-channel routing layer (Visual, Sound, Touch, Motion, Comprehension). |

These projections ensure **agreement without collapse**, meaning two peers can hold different orientations (e.g., one seeing a vertex traversal while another sees an edge traversal) while remaining bound to the same accepted $o---o$ center.
---

## You
`2026-06-13 21:05` | `id:260613-2105` ^260613-210519

I have those modeules declared in this documentation repoi think we can make the 0mi-* react components that work on base64 binary scripts like vmscrips or worker scrips as base64 for the tetragrammatron f-expression metat-circulation

I searched the repo canon and reference material. The short version: your framing is mostly right, but the repo currently canonizes only some of those exact Omi-* names. Several others are good inferred surface names, but not yet canonical by exact term.
Strong Canonical Hits
These are explicitly defined or repeatedly grounded:
Surface	Repo status	Evidence
o---o	canonical root relation	[GLOSSARY.md (line 2103)](/root/omi-portal/GLOSSARY.md:2103)
Omi-Form	explicit	[GLOSSARY.md (line 2159)](/root/omi-portal/GLOSSARY.md:2159)
Omi-Glyph	explicit	[GLOSSARY.md (line 2167)](/root/omi-portal/GLOSSARY.md:2167)
Omi-Hash	explicit	[GLOSSARY.md (line 2183)](/root/omi-portal/GLOSSARY.md:2183)
Omi-Image	explicit	[GLOSSARY.md (line 2189)](/root/omi-portal/GLOSSARY.md:2189)
Omi-Matrix	explicit	[GLOSSARY.md (line 2203)](/root/omi-portal/GLOSSARY.md:2203), [docs/omi-nomogram.md (line 202)](/root/omi-portal/docs/omi-nomogram.md:202)
Omi-Gnomon	explicit	[GLOSSARY.md (line 2171)](/root/omi-portal/GLOSSARY.md:2171), [docs/omi-nomogram.md (line 203)](/root/omi-portal/docs/omi-nomogram.md:203)
Omi-Compass	explicit	[GLOSSARY.md (line 2149)](/root/omi-portal/GLOSSARY.md:2149), [docs/omi-nomogram.md (line 331)](/root/omi-portal/docs/omi-nomogram.md:331)
Omi-Ring	explicit	[GLOSSARY.md (line 2244)](/root/omi-portal/GLOSSARY.md:2244), [docs/omi-nomogram.md (line 277)](/root/omi-portal/docs/omi-nomogram.md:277)
Omi-World	explicit	[GLOSSARY.md (line 2272)](/root/omi-portal/GLOSSARY.md:2272), [docs/omi-plane-capsule.md (line 1100)](/root/omi-portal/docs/omi-plane-capsule.md:1100)
Omi-Nomogram	canonical declarative function-scale surface	[docs/omi-nomogram.md (line 751)](/root/omi-portal/docs/omi-nomogram.md:751)
Omi-Gauge	computed witness/spatial resolver	[GLOSSARY.md (line 2175)](/root/omi-portal/GLOSSARY.md:2175), [docs/omi-gauge-autonomous-function-space.md (line 60)](/root/omi-portal/docs/omi-gauge-autonomous-function-space.md:60)
Omi-SlideRule	operational face of Nomogram	[GLOSSARY.md (line 2254)](/root/omi-portal/GLOSSARY.md:2254), [docs/omi-nomogram.md (line 236)](/root/omi-portal/docs/omi-nomogram.md:236)
Omi-CONS, CAR, CDR, CID	post-address epistemic carrier	[GLOSSARY.md (line 2129)](/root/omi-portal/GLOSSARY.md:2129)
Omi-Receipt	lawful relation witness	[GLOSSARY.md (line 2238)](/root/omi-portal/GLOSSARY.md:2238)

Core Canon Pattern
The strongest existing canonical chain is:
Omi-Gauge computes the cell.
Omi-Nomogram selects how it is read.
Omi-Matrix instantiates the relation field.
Omi-Gnomon orients the result.
Receipt accepts.
That is directly stated in [docs/omi-nomogram.md (line 198)](/root/omi-portal/docs/omi-nomogram.md:198) and repeated in the final canon path at [docs/omi-nomogram.md (line 791)](/root/omi-portal/docs/omi-nomogram.md:791).
The authority boundary is also clear: projections do not validate. RULES.omi, FACTS.omi, and the 8-segment pointer remain authority; symbolic/visual carriers are projection surfaces only. See [docs/omi-object-model.md (line 285)](/root/omi-portal/docs/omi-object-model.md:285).
Names That Are Conceptually Supported But Not Exact Canon Terms
These ideas exist, but I did not find exact canonical Omi-* entries for them:
Proposed name	Current repo grounding
Omi-Map	coordinate/body mapping exists through Q_xy, gauge cells, Base36/socket projection, and data-omi projection strings, but exact Omi-Map is not defined.
Omi-Mirror	mirror/chirality exists, including 0xNM ↔ 0xMN, but exact Omi-Mirror is not an explicit surface entry.
Omi-Clock	timing/replay exists through factorial replay ring, 5040 slots, sexagesimal gate 0x3C, and cadence, but exact Omi-Clock is not canonized.
Omi-Light	visual/color/opacity surfaces exist through canvas color routing, JAB/polychromatic carrier, voxel projection, but exact name absent.
Omi-Sound	cadence/rhythm language exists, but exact surface absent.
Omi-Portal	the repo is omi-portal and has portal pages/binder code, but exact declarative surface definition Omi-Portal is not in the glossary.
Omi-Sense	sensory routing is broadly compatible with projection/event surfaces, but exact term absent.

Names I Did Not Find As Canonical
I did not find exact canonical definitions for:
Omi-Automaton
Omi-Actor
Omi-Observer
Omi-Pyramid
Omi-Amulet
Omi-Dome
Omi-Base
Omi-FS / Imo-FS
Omi-Node / Imo-Node
Omi-Frame / Imo-Frame
There are related concepts: “automaton” appears in dev-docs/tetragrammatron-meta-memory-automaton.md and manifest references; DOM/custom element projection exists through data-omi, data-imo, <omi>, <imo>, and <o> in [PROJECTIONS.md (line 130)](/root/omi-portal/PROJECTIONS.md:130). But the exact surface names above are not currently canonicalized.
Best Current Canon List
If you want the list to match the repo truth today, I’d phrase it as:
Canonical / explicit:
Omi-Form
Omi-Glyph
Omi-Hash
Omi-Image
Omi-Matrix
Omi-Gnomon
Omi-Compass
Omi-Ring
Omi-World
Omi-Gauge
Omi-Nomogram
Omi-SlideRule
Omi-CONS
Omi-Receipt
Omi-Point
Omi-Gate
Omi-Shadow
Omi-Bidi
Omi-Torus
Omi-Voxel
Omi-Jab
Omi-Mesh
Omi-Tape
Omi-Lisp
Omi-Alist
And then mark the rest as proposed projection names until promoted into canonical docs/tests.

---


This is clearly the ancestor of:

```text id="o9263o"
raw boot kernel
Tetragrammatron meta-memory
TrackLog / WOLog
Omi-Gnomon
Omi-Tape
Omi-Receipt
